pipeline {
    stages {
	stage ('checkout scm') {
		steps {
			checkout scm
		}}
	}

	stage('PHP test') {
            steps {
                sh ""
            }
        }

	stage('NodeJS test') { 
		steps {
			sh ""
		}
	}
    }
}
